<%= form_for(@sr_note) do |f| %>
  <% if @sr_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sr_note.errors.count, "error") %> prohibited this sr_note from being saved:</h2>

      <ul>
      <% @sr_note.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :note %><br />
    <%= f.text_area :note %>
  </div>
  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
